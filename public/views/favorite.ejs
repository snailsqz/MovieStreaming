<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NetBoss</title>
    <link rel="stylesheet" href="../css/index.css">
    <style>
      .favtxt{
        color: #212427;
        font-size: x-large;
        font-weight: 500;
      }
    </style>  
  </head>
  <body>
    <header> 
      <a href="/">Home</a>
      <% if (moviedata.userName != "" && moviedata.roles == "Admin") { %>
      <a href="/create">Add Movie</a>
      <a href="/movieupdate">Update Movie</a>
      <a href="/moviedelete">Delete Movie</a>
      <% }%>
      <% if (moviedata.roles == "User" ){ %>
      <a href="/favorite/<%=moviedata.user_id%>">Favorite</a>
      <% }%>
      <% if (!moviedata.userName) { %>
      <a href="/register">Register</a>
      <a href="/login">Login</a>
      <% } %>
      <% if (moviedata.userName) { %>
      <a href="/logout">LogOut</a>
      <% } %>
    </header>
    <% if (moviedata.userName) { %>
      <p class="welcome">Welcome, <%= moviedata.userName %>!</p>
    <% } else { %>
      <p class="welcome">Welcome!</p>
    <% } %>
    <div class="mainbox">
      <% if (favoriteMovie.length == 0) { %>
        <p class="favtxt">No favorite now! Let's add something</p>
      <% } %>
      <% for(let i = 0; i < favoriteMovie.length; i++){%>
        
        <div class="imgitem">
          <p><%=favoriteMovie[i]%></p>
          <a href="/movie/<%=movies[favoriteMovie[i] - 1].movie_id%>"><img src="/images/<%= movies[favoriteMovie[i] - 1].imageFile %>"></a>
          <a href="/movie/<%=movies[favoriteMovie[i] - 1].movie_id%>"><%= movies[favoriteMovie[i] - 1].title %></a>
        </div>
      <%}%>
    </div>
  </body>
</html>

